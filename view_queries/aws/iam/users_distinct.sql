
-- AUTOGENERATED BY VIEW-GENERATOR, DO NOT MODIFY
CREATE OR REPLACE VIEW distinct_iam_users AS 
WITH
	filter AS (
	SELECT
		*
	, "max"(report_time) OVER (PARTITION BY user_id) oldest_report_time
	FROM
		iam_users
) 
SELECT *
FROM
	filter
WHERE (report_time = oldest_report_time)