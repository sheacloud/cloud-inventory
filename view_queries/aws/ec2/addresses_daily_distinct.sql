
-- AUTOGENERATED BY VIEW-GENERATOR, DO NOT MODIFY
CREATE OR REPLACE VIEW daily_distinct_ec2_addresses AS 
WITH
	filter AS (
	SELECT
		*
	, "max"(report_time) OVER (PARTITION BY allocation_id, report_date) oldest_report_time
	FROM
		ec2_addresses
) 
SELECT *
FROM
	filter
WHERE (report_time = oldest_report_time)